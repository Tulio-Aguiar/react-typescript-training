Shared custom React hooks — reused across two or more features or pages.
Hooks used by a single component stay co-located with that component.

Examples: `useDebounce.ts`, `useLocalStorage.ts`, `useMediaQuery.ts`
