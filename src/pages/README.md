Route-level components — one file per application screen/view.
Pages consume features and layouts; they contain minimal logic themselves.

Examples: `HomePage.tsx`, `LoginPage.tsx`, `DashboardPage.tsx`
