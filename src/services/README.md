API calls and external service integrations.
Components stay clean and declarative — all async data logic lives here.

Examples: `api.ts` (axios/fetch base instance), `userService.ts`, `authService.ts`
