Reusable primitive UI components — Button, Input, Modal, Badge, etc.
Each component lives in its own subfolder:

```
components/
└── ui/
    └── Button/
        ├── Button.tsx
        ├── Button.test.tsx
        └── index.ts
```
